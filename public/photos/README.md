# Photos

Drop the selected photos here (JPEG or WebP, long edge ≤ 1600 px), then set
`src` on the matching frame in `content/photos.ts`, for example
`src: "/photos/01.jpg"`, and write a real `alt`.

Frames without a `src` render as numbered placeholders.
