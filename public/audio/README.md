# Audio

Drop the exported masters here (MP3 or AAC, ~192 kbps is plenty), then point
`src` at them in `content/music.ts`, for example `src: "/audio/the-handoff-v2.mp3"`.

Until a track has a `src`, the player shows "[ preview coming ]" instead of playing.
